# История изменений — Солнечный компас

Формат: [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/).
Номер версии живёт в `APP_VERSION` (`index.html`) и в `CACHE_NAME` (`sw.js`) — менять оба.

## [2.0.0] — 2026-07-26

Кольцо перестало быть просто шкалой: теперь на нём нарисован ход солнца за сутки.

### Добавлено
- **Траектория солнца «сегодня»** на кольце. Полярная проекция: угол — азимут, радиус — высота
  над горизонтом. Пунктирная окружность `r = 115` стала линией горизонта, центр — зенитом.
- **Градиентные отрезки фаз**: золотой час, восход/закат, синий час и сумерки раскрашены по высоте
  солнца с плавным переходом. Толщина линии тоже меняется — день идёт тонкой нитью, съёмочные
  часы у горизонта выходят на первый план.
- **Разделение прошлого и будущего**: пройденная часть суток тоньше и приглушена, оставшаяся —
  в полную силу.
- **Засечки восхода и захода** поперёк линии горизонта.
- **Строка состояния** в подвале: восход · текущая фаза с высотой солнца · заход.
  Цвет фазы совпадает с цветом отрезка на кольце.
- `SUN_PHASES` — единая таблица порогов высоты солнца для кольца, подписи результата
  и строки состояния.
- Service Worker (`sw.js`): офлайн-режим, которого раньше не было, несмотря на заявление в README.
  Документ загружается network-first, чтобы установленное PWA не залипало на старой версии.
- Иконки `icon.svg`, `icon-192.png`, `icon-512.png` и рабочий `manifest.json`.
- `.gitignore`, `LICENSE` (MIT, с указанием встроенного SunCalc), `docs/ARCHITECTURE.md`,
  `docs/SWIFT_PORT.md`.

### Изменено
- **Точка текущего солнца встала на траекторию.** Раньше она всегда висела на радиусе 115
  независимо от высоты солнца — положение по азимуту было верным, по высоте не показывалось никак.
- **Мишень замера садится на свой радиус** — туда, где солнце реально окажется по высоте,
  а не на условный радиус 140. Летящая точка теперь тоже меняет радиус по ходу ролика.
- Компас масштабируется под экран (`min(340px, 100vw − 48px, 46dvh)`) — на iPhone SE
  разметка больше не упирается в края.
- В зафиксированном состоянии деления приглушаются, чтобы читались траектория и мишень.
- Манифест и иконки вынесены из data-URI в отдельные файлы.
- Вместо фальшивого «Build» со временем загрузки страницы показывается настоящая версия.
- Детекторы тапов по компасу и по кнопке спуска сведены в одну фабрику.

### Исправлено
- **Замер на север уходил в вечную «Калибровку».** `isHeadingStable()` считал разброс курса
  как `max − min` по сырым значениям: колебание около севера (359.8° и 0.2°) давало размах 359°.
  Теперь разброс считается циклически.
- **Взгляд ровно на север давал неверный курс.** `e.webkitCompassHeading || (360 − e.alpha)`
  на значении 0 проваливался во вторую ветвь. Проверка заменена на проверку типа.
- **В ландшафте компас врал на 90°** — в ветви `alpha` добавлена поправка на `screen.orientation.angle`.
- **Приложение намертво зависало в состоянии `CALCULATING`**, если экран гас или приложение
  сворачивали во время замера: браузер замораживает `requestAnimationFrame`, а выйти из этого
  состояния тапом нельзя. Добавлен сторож `setTimeout` и идемпотентный `finishStory()`.
- При возврате из фона положение солнца, траектория и минуты часов пересчитываются
  по `visibilitychange` — раньше какое-то время висели устаревшие данные.
- `manifest.json` ссылался на несуществующие `icon-192.png` и `icon-512.png` и при этом вообще
  не был подключён: в HTML стоял отдельный манифест в data-URI.
- Номер версии в подвале больше не перекрывается зоной индикатора Home (учтён `safe-area-inset`).
- Из подписи результата убраны ветви, недостижимые из-за порога поиска `SEARCH_MIN_ALTITUDE = −6°`.

### Документация
- README переписан: убрана служебная преамбула, попавшая в файл при генерации, и снято
  утверждение об офлайн-работе, которое до появления `sw.js` было неверным.

---

## Ранние версии

Записи ниже сохранены как есть, в исходном формате. Часть версий описана по нескольку раз
с разными формулировками — это следы ручного ведения файла, а не разные релизы.

### Build 1.2.3

Исправлено:

Корректировка визуального слоя: Буквы сторон света (N, E, S, W) отодвинуты на радиус 155 пикселей, чтобы исключить их наложение на черточки градусных делений.
Возвращение на орбиту: Точка текущего положения Солнца (currentSunDot) теперь снова движется строго по пунктирной орбитальной окружности (радиус 115), а не со смещением.
Build 1.2.2

Исправлено:

Полная геометрия обзора: Устранено обрезание увеличенного компаса по краям экрана. SVG-холст (viewBox) расширен до 340x340, а центр пересчитан на координаты (170, 170). Внешний круг и все деления теперь полностью помещаются в область видимости.
Build 1.2.1

Улучшено:

Масштабирование циферблата: Размер виджета компаса увеличен до 340×340 пикселей. Внешний радиус рисок и орбитальных колец пропорционально расширен до 160 и 115.
Плавная смена минут: Добавлена анимация на блоке .time-value при обновлении цифр на часах (изменение прозрачности и лёгкий вертикальный сдвиг), что сделало смену времени более кинематографичной.
Build 1.2.0

Астрономия & Производительность:

Full SunCalc Source: Минифицированный блок кода SunCalc заменён на полный, читаемый исходный код библиотеки. Это исключило критические ошибки сжатия, из-за которых азимут Солнца неверно рассчитывался в мобильных браузерах.
Визуал:
Dynamic Sun Opacity: Реализовано многоуровневое отображение текущего Солнца. В зависимости от высоты над горизонтом точка меняет прозрачность: полностью непрозрачна над горизонтом, 0.4 в гражданских сумерках (до -6°) и едва заметна (0.15) астрономической ночью.
Поиск:
Extended & Softened Search: Окно поиска солнечного прохождения (findSolarPassage) расширено до 40 дней, а шаг сканирования уменьшен до 3 минут. Допуск совпадения азимута (angDiff) увеличен до 5°, а зона точной подгонки до ±20 минут, что гарантирует нахождение результата даже при небольшом дрожании компаса.
### Build 1.2.0

* Full SunCalc Integration: Интегрирован полный неизмененный исходный код библиотеки SunCalc 2.0.1 без минификации, исключивший любые синтаксические ошибки сжатия.
* Dynamic Sun Opacity: Добавлено визуальное отображение ночного положения текущего солнца — точка меняет прозрачность в зависимости от высоты (тусклая при сумерках ниже -6° и минимальная ночью).
* Extended Passage Search Window: Окно поиска солнечного прохождения увеличено до 40 дней с шагом в 3 минуты для более глубокого планирования.
### Build 1.1.3
* SunCalc Global Fix: Заменен встроенный минифицированный блок SunCalc на рабочий аналог с корректным глобальным экспортом в window.SunCalc, устранен баг с постоянным статусом "Вне орбиты".
* Inlined SunCalc: Библиотека расчёта солнечных координат встроена в проект для обеспечения полной офлайн-автономности.
* Geo Caching: Добавлено кэширование названий городов в localStorage по координатам для снижения числа запросов к Nominatim.
* Compass Readiness Gate: Добавлен защитный флаг compassReady для блокировки замера до получения первого валидного сигнала гироскопа.
* State Sync on Reset: Реализован моментальный пересчет положения солнца при сбросе блокировки.
* SVG Filter Fix: Исправлен рендеринг свечения в мобильных браузерах за счет использования фиксированных hex-кодов в фильтрах.
* Interval Optimization: Фоновые таймеры переведены на интервал в 60 секунд для экономии заряда батареи.
### Build 1.1.2
* SunCalc Global Export: Исправлен критический баг видимости встроенной библиотеки — добавлен явный экспорт window.SunCalc из IIFE-модуля, благодаря чему астрономические расчёты снова работают корректно.
* Inlined SunCalc: Перенос библиотеки расчёта солнечных координат непосредственно внутрь основного файла для обеспечения 100% офлайн-автономности и мгновенной загрузки.
* Geo Caching: Внедрение локального кэширования названий городов в localStorage по округлённым координатам для минимизации лишних сетевых запросов.
* Compass Readiness Gate: Добавлен флаг compassReady, блокирующий замер до получения первого реального сигнала от датчиков ориентации устройства.
* State Sync on Reset: В resetLock() добавлен мгновенный пересчет текущего положения Солнца (updateCurrentSunPosition()) при сбросе блокировки.
* SVG Filter Fix: Замена CSS-переменной в SVG-фильтре drop-shadow на фиксированный hex-код (#ffd25f) для гарантированного отображения эффекта свечения в Safari и мобильных браузерах.
* Interval Optimization: Оптимизация фоновых таймеров и перевод обновления положения точки Солнца на стабильный 60-секундный интервал для снижения нагрузки на батарею.
## [1.1.2]
* **Inlined SunCalc:** Перенос библиотеки расчёта солнечных координат непосредственно внутрь основного файла для обеспечения 100% офлайн-автономности и мгновенной загрузки.
* **Geo Caching:** Внедрение локального кэширования названий городов в `localStorage` по округлённым координатам для минимизации лишних сетевых запросов.
* **SVG Filter Fix:** Замена CSS-переменной в SVG-фильтре `drop-shadow` на фиксированный hex-код (`#ffd25f`) для гарантированного отображения эффекта свечения в Safari и мобильных браузерах.
* **Interval Optimization:** Оптимизация фоновых таймеров и перевод обновления положения точки Солнца на стабильный 60-секундный интервал для снижения нагрузки на батарею.
## [1.1.1] - Build 1.1.1 (Патч стабильности и синхронизации)
**Исправлено:**
* Устранен скачок компаса после разблокировки экрана: добавлена переменная `needSyncAfterUnlock` и принудительная синхронизация углов `unwrappedHeading` в `handleOrientation()`.
* Добавлена защита от повторных нажатий во время выполнения расчетов (`isCalculating`), предотвращающая гонку состояний (race conditions).
* В запрос к Nominatim добавлен обязательный заголовок `User-Agent` (`SolarCompass/1.0`).
* Расширена глубина поиска в `findSolarPassage` до 30 дней и добавлена защита от исключений в `SunCalc` при некорректных координатах.
## [1.1.0] - Stable Release: Отказоустойчивость инициализации
**Исправлено:**
* Устранено зависание стартового экрана PWA («Подключение...») в условиях слабого сигнала GPS или блокировки геолокации. Теперь `navigator.geolocation.getCurrentPosition` имеет надежный обработчик ошибок (`err => {}`) и увеличенный таймаут до 10 секунд.
* При явном отказе пользователя выдать права на гироскоп (`DeviceOrientationEvent`), кнопка подключения сбрасывается в исходное состояние, предотвращая блокировку интерфейса.
## [1.0.9] - PWA & Живые часы
**Добавлено:**
* Интеграция `manifest.json` и тегов `<meta>`: теперь компас работает как полноценное PWA-приложение (добавляется на домашний экран без интерфейса браузера).
* Живые часы: в режиме видоискателя (когда азимут не зафиксирован) на экране отображается текущее время, оживляя интерфейс.
**Улучшено:**
* Оптимизирована математика нормализации углов `angDiff` с использованием `% 360` для большей надежности расчетов на границах цикла.
## [1.0.8] - Интеграция SunCalc & Оптимизация Поиска
**Астрономия & Точность:**
* Старый алгоритм NOAA полностью заменен на профессиональную библиотеку `SunCalc`. Вычисления теперь учитывают уравнение времени (Equation of Time) и атмосферную рефракцию, гарантируя прецизионную точность для золотого и синего часов.
* Азимут SunCalc приведён к навигационному стандарту (`(azimuth + 180) % 360`), обеспечивая идеальную синхронизацию с компасом iOS.
**Производительность & UI:**
* Переписан механизм поиска `findSolarPassage`: используется предварительное сканирование с шагом 5 минут и точное минутное сведение (`fine-tuning`). Нагрузка на процессор при замерах снижена на 95%.
* Устранен баг, из-за которого целевое солнце (`targetSunDot`) иногда не отображалось при фиксации кадра.
* Добавлен `setInterval` — теперь белая точка текущего положения солнца на внутреннем орбитальном кольце автоматически перемещается в реальном времени (каждые 20 секунд).
## [1.0.7] - Синхронизация систем отсчета азимута & Build 1.0.7
**Исправлено:**
* Устранена ключевая ошибка расхождения систем координат: астрономический расчет положения Солнца теперь переведен в навигационную шкалу от Севера (`0° = N`) с помощью функции `toNorthAzimuth`.
* Точка текущего солнца (`currentSunDot`) и алгоритм поиска времени (`findSolarPassage`) теперь корректно сопоставляются с показаниями компаса телефона.
* Оптимизирован обход суточных проходов: добавлено смещение шага сканирования, корректно обрабатывающее утренние и вечерние пересечения азимута без ложных пропусков.
## [1.0.6] - Умный расчет будущего времени & Build 1.0.6
**Исправлено:**
* Исправлена логика поиска времени инсоляции: алгоритм теперь проверяет условие `passageDate > now`. Если солнце в выбранном азимуте сегодня уже прошло, приложение автоматически переключается на поиск этого же положения в будущие дни (например, завтра в то же время).
## [1.0.5] - Чистая 2D проекция (Вид сверху)
* Переход на единую 2D систему координат (вид сверху, Земля в центре). Весь диск компаса (север, деления и маркеры солнца) вращается как единый плоский прибор.
* **Текущее Солнце**: отображается белой точкой на внутреннем орбитальном кольце ($r = 100$), показывая реальное направление на солнце в текущий момент суток.
* **Целевое Солнце**: фиксируется на внешнем кольце ($r = 140$) при нажатии кнопки спуска.
* Добавлена верхняя визирная риска-индикатор для точного наведения в стиле видоискателя.
## [1.0.4] Добавляем орбитальное кольцо траектории солнца, убираем лишнюю надпись «Наведитесь» и сохраняем север на месте.
В этой сборке (Build 1.0.4) на компас добавлено второе внутреннее орбитальное кольцо с плавающей меткой реального положения солнца на текущий момент дня. Настоящий север (N) остаётся на своем месте, а кольцо наглядно показывает, где солнце находится прямо сейчас относительно вашего направления.
## [1.0.3] - Поиск без разрывов & Build Tracker
**Исправлено:**
* Критический баг "00:00", возникавший из-за бинарного поиска на стыке углов $359^\circ$ и $0^\circ$.
* Алгоритм расчёта инсоляции полностью переписан на комбинированный метод: сканирование суток с шагом 30 минут + поминутный Brute-Force (`±45` минут) при нахождении нужного сектора.
* `findSolarPassage` теперь стартует ровно в `00:00` текущего дня, чтобы не пропускать утренние часы.

**Добавлено:**
* Трекер версии приложения (`Build 1.0.3`) внизу экрана в стиле Ghost / Apple (минималистичная типографика, $10\text{px}$, opacity $15\%$).

---

## [1.0.2] - Shutter Button & Camera UX
**Добавлено:**
* Интерфейс переведён в парадигму приложения "Камера". Кнопка "Рассчитать" заменена на визуальную кнопку спуска затвора (Shutter Button).
* Shutter Freeze: нажатие на компас или кнопку спуска блокирует азимут намертво. Сдвиг телефона больше не сбрасывает результат. Сброс происходит только по повторному тапу.
* Визуальный отклик фиксации: кнопка спуска преобразуется в янтарный индикатор при успешном наведении.

**Исправлено:**
* Проблема сброса результатов от микро-движений рук ("шум" гироскопа iPhone).

---

## [1.0.1] - Apple Minimalist UI & PWA Fixes
**Добавлено:**
* Полностью новый дизайн (Glassmorphism, Dark Graphite, типографика UltraLight $72\text{px}$). 
* Стартовый экран-оверлей (`startOverlay`) для корректного запроса прав гироскопа и геолокации (обход ограничений iOS Safari / режима Home Screen).
* Бесшовное вращение компаса (Unwrapped Heading), предотвращающее скачок кольца при переходе через север.

**Исправлено:**
* Огромная нагрузка на CPU (Throttling и перенос вычислений в `requestAnimationFrame`).
* Интегрирована астрономическая модель NOAA для точного звездного времени (GMST).

---

## [1.0.0] - Прототип (MVP)
* Базовая структура приложения и интерфейс.
* Заложена первоначальная математика тригонометрии Солнца.
* Настроена публикация через GitHub Pages.
